import Logo from 'src/components/extended/Logo';

// ==============================|| MAIN LOGO ||============================== //

const LogoSection = (): JSX.Element => {
  return <Logo />;
};

export default LogoSection;
