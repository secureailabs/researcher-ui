import { Outlet } from 'react-router-dom';

// ==============================|| LOGIN LAYOUT ||============================== //

const LoginLayout = (): JSX.Element => (
  <>
    <Outlet />
  </>
);

export default LoginLayout;
